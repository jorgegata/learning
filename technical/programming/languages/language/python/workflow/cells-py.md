# Jupyer-style inside a .py

The goal is to run cells inside a .py file as it is a jupyer notebook. 

> The main method is doing a # %% separator for the different coding blocks

```python

# %%
import pandas as pd
import matplotlib.pyplot as plt

print(f"Hello, this is a cell test")

```